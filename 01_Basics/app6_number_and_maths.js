const balance = 400; // int
const name = "shubham" // string
const salary = Number(50000)
const lulli = 23.983883838
const hundreds = 1000000 
console.table([balance,name,salary,lulli.toFixed(1),lulli.toPrecision(2),hundreds.toLocaleString('en-IN')])

 